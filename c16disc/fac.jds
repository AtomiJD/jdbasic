func factorial(a)
    if a > 1 then
        r=factorial(a-1)
        r=r*a
    else        
        r=a
    endif
    return r
endfunc

print "Factorial from 8..."
fac =  factorial(8)
print "Result: ", fac
